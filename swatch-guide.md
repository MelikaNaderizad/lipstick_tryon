# راهنمای عکس‌گیری سواچ رژ لب

این راهنما برای فروشنده‌هاست: عکسی که برای هر رنگ (Shade) در `/review` آپلود می‌شه باید طبق این
قالب باشه تا سیستم بتونه رنگ خالص رژ رو درست استخراج کنه. جایگزین فایل PDF قدیمیه.

## قالب پیش‌فرض عکس

عکس باید **دو ناحیه‌ی کنار هم** داشته باشه، در یه قاب افقی:

```
┌─────────────────────────────────────────────┐
│                                               │
│   [ پوست خالی ]        [ رژ کشیده‌شده ]       │
│      (چپ)                  (راست)            │
│                                               │
└─────────────────────────────────────────────┘
```

- **سمت چپ (تقریباً از ۱۰٪ تا ۴۵٪ عرض عکس، از ۳۰٪ تا ۷۰٪ ارتفاع)**: پوستِ کاملاً خالی، بدون رژ.
  می‌تونه پشت دست یا مچ باشه.
- **سمت راست (تقریباً از ۵۵٪ تا ۹۰٪ عرض، از ۳۰٪ تا ۷۰٪ ارتفاع)**: رژ روی همون پوست، به‌صورت یه
  لکه/سواچ کشیده‌شده.

اگه دقیقاً همین قالب رو رعایت کنی، توی `/review` می‌تونی از کادرهای پیش‌فرض استفاده کنی و
نیازی به کشیدن دستی کادر نیست. اگه با آرایش دیگه‌ای عکس گرفتی، کادر آبی (پوست) و کادر سبز (رژ)
رو خودت روی عکس بکش.

## نکات مهم درباره‌ی خودِ لکه‌ی رژ

- لکه باید **حداقل یه بخش ضخیم و کاملاً کدر** داشته باشه — جایی که رژ چند لایه روی هم کشیده شده و
  رنگ پوست زیرش اصلاً دیده نمی‌شه. سیستم دقیقاً همین بخش ضخیم رو پیدا می‌کنه و رنگ خالص رو از
  اونجا می‌خونه؛ لبه‌های نازک و کم‌رنگ لکه رو نادیده می‌گیره.
- اگه محصول ذاتاً **شفاف/نیمه‌شفافه** (مثل گلاس یا بالم) و حتی ضخیم‌ترین بخشش هم پوست زیرش رو
  کمی نشون می‌ده، این طبیعیه — سیستم هشدار `swatch_may_be_sheer` می‌ده؛ در این حالت بهتره در
  تنظیمات آپلود مقدار «پوشش/opacity» محصول رو هم مشخص کنی تا رنگ درست unmix بشه.
- کادر سبز (رژ) لازم نیست دقیقاً فقط رنگ رو در بر بگیره — می‌تونه یه مستطیل شل دور لکه باشه،
  حتی اگه بخشی از پوست خالی هم داخلش بیفته. سیستم خودش پیگمنت رو از پوست جدا می‌کنه. فقط اگه
  بیشتر از ۴۰٪ کادر پوست باشه هشدار `swatch_box_loosely_cropped` می‌گیری (بازم رنگ درست درمیاد،
  فقط یعنی دفعه‌ی بعد می‌تونی دقیق‌تر بکشی).
- از افتادن **برق/هایلایت گلاس** زیاد روی لکه نگران نباش؛ سیستم پیکسل‌های خیلی روشن‌تر از بقیه
  رو به‌عنوان هایلایت کنار می‌ذاره. ولی بهتره لکه رو طوری بگیری که حداقل بخشی از سطحش مات/بدون
  انعکاس مستقیم نور باشه.

## نور و کالیبراسیون رنگ

- نور طبیعی یا سفید (نه زرد/نارنجی شدید) بهترین نتیجه رو می‌ده. نور رنگی، اریب یا خیلی کم می‌تونه
  رنگ نهایی رو منحرف کنه.
- برای بیشترین دقت، یه **کارت خاکستری خنثی** (یا هر سطح خاکستری یکدست) رو گوشه‌ی بالا-چپ عکس
  (تقریباً ۲٪ تا ۱۴٪ عرض/ارتفاع) بذار و موقع آپلود کادرش رو هم مشخص کن. این تنها راهیه که کجی
  رنگ نور (نه فقط روشنایی) رو کاملاً اصلاح می‌کنه.
- اگه کارت خاکستری نداری، حداقل **تناژ پوست خودت رو در فرم آپلود انتخاب کن** (به‌جای «حدس
  خودکار»). حدس خودکار در نور کم یا غیرعادی می‌تونه پوست روشن رو تیره‌تر از واقعی بفهمه.
- از نورهای مستقیم و بسیار شدید (باعث سوخته/overexposed شدن لکه) و از نورهای خیلی کم
  (لکه در حد نویز تاریک) پرهیز کن. سیستم به ترتیب هشدار `swatch_overexposed` یا
  `swatch_too_dark` می‌ده.

## فرمت و حجم فایل

- فرمت‌های قابل‌قبول: **JPEG، PNG، WebP**.
- حداکثر حجم فایل: **۱۰ مگابایت**.
- رزولوشن خیلی بالا مشکلی نداره (خودکار کوچیک می‌شه تا ضلع بلندش حداکثر ۱۶۰۰ پیکسل بشه)، ولی
  عکس با رزولوشن بی‌نهایت پایین یا خیلی فشرده کیفیت استخراج رو کم می‌کنه.
- اگه گوشی/دوربینت پروفایل رنگی خاص (مثلاً Display P3 روی آیفون) ثبت کنه، سیستم خودش به sRGB
  تبدیلش می‌کنه — نیازی به تنظیم دستی نیست.

## چک‌لیست سریع قبل از آپلود

- [ ] پوست خالی و لکه‌ی رژ هر دو در یه عکس، کنار هم
- [ ] لکه حداقل یه بخش ضخیم/کاملاً کدر داره (مگر محصول ذاتاً شفاف باشه)
- [ ] نور یکنواخت، نه خیلی تاریک نه خیلی سوخته
- [ ] در صورت امکان، کارت خاکستری در گوشه‌ی عکس
- [ ] تناژ پوست (Anchor) در فرم آپلود انتخاب شده — یا حداقل کارت خاکستری هست
- [ ] فایل JPEG/PNG/WebP و زیر ۱۰ مگابایت

## اگه بعد از آپلود هشدار گرفتی

| هشدار                         | یعنی چی                                          | چیکار کنی                                                    |
| ----------------------------- | ------------------------------------------------ | ------------------------------------------------------------ |
| `swatch_not_uniform`          | داخل کادر سبز رنگ‌ها خیلی پراکنده‌ن              | کادر رو دقیق‌تر روی لکه بکش، یا لکه‌ی یکدست‌تری بگیر         |
| `swatch_patch_small`          | کادر سبز خیلی کوچیکه                             | کادر بزرگ‌تر بکش یا از فاصله‌ی نزدیک‌تر عکس بگیر             |
| `swatch_overexposed`          | لکه بیش‌ازحد روشن/سوخته‌ست                       | نور رو کم کن یا از فلاش مستقیم پرهیز کن                      |
| `swatch_too_dark`             | لکه بیش‌ازحد تاریکه                              | نور رو بیشتر کن                                              |
| `swatch_box_loosely_cropped`  | کادر سبز اکثرش پوست بوده، سیستم خودش کنار گذاشتش | نتیجه معمولاً درسته؛ دفعه‌ی بعد دقیق‌تر بکش                  |
| `swatch_pigment_not_found`    | داخل کادر سبز چیزی متفاوت از پوست پیدا نشد       | احتمالاً کادر رو اشتباهاً روی پوست خالی کشیدی — دوباره چک کن |
| `swatch_may_be_sheer`         | حتی ضخیم‌ترین بخش لکه هم کاملاً کدر نیست         | اگه محصول واقعاً نیمه‌شفافه، مقدار opacity رو در فرم مشخص کن |
| `lighting_correction_clamped` | نور عکس خیلی نامتعادل بوده، تصحیح به سقفش رسیده  | نور عکس رو طبیعی‌تر کن و دوباره امتحان کن                    |
