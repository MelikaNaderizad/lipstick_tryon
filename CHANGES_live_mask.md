# تغییرات: ماسک لب واقعی‌تر برای پیش‌نمایش لایو

این زیپ فقط دو فایل رو عوض می‌کنه (بقیه‌ی پروژه دست‌نخورده‌ست). از ریشه‌ی ریپو
extract کن؛ روی فایل‌های قبلی می‌شینن و جایگزین می‌شن:

  src/back/app/color_engine/live_render.py
  src/back/app/routers/live.py

## خلاصه‌ی تغییرات

1. **`refine_edges=True` در `live.py`** — ماسک لب حالا با خودِ فریم دوربین
   هماهنگ می‌شه (guided filter روی کانال a\*/L) تا لبه‌ش دقیقاً به مرز واقعی
   لب/پوست بچسبه. این مکانیزم فقط می‌تونه ماسک رو تنگ‌تر کنه نه بزرگ‌تر، پس
   ریسکی برای بیرون‌زدگی نداره.
2. **`MASK_GROW` و `FEATHER_FRAC` کمتر شدن** (`0.015→0.008` و `0.02→0.012`)
   تا ماسک از اول هم بزرگ‌تر از لب واقعی کشیده نشه.
3. **آستانه‌ی اعمال رنگ در `blend_lip_color` بالا رفت** (`m > 0.02` →
   `m > 0.25`) + شکل‌دهی غیرخطی opacity، تا حاشیه‌ی محو ماسک (که اغلب خودِ
   پوسته‌ست) رنگ نگیره — این همون چیزی بود که باعث می‌شد پوست اطراف لب تیره‌تر
   دیده بشه.
4. **`chroma_keep` برای `liquid`/`stick` بالاتر رفت** (بافت طبیعی لب بیشتر از
   زیر رنگ دیده می‌شه، حس مصنوعی/پلاستیکی کمتر می‌شه).

هیچ‌کدوم از این تغییرات به مدل دیتابیس، anchor، یا مسیر استخراج رنگ سواچ ربطی
ندارن — فقط رندر لایو (`/ws/live-tryon`) رو تحت تأثیر قرار می‌دن.

## چک کردن سریع بعد از اعمال

```bash
cd src/back
pytest tests/test_live_render.py
```

## به‌روزرسانی دوم (بازخورد: «خیلی مصنوعیه» — شبیه استیکر/ایموجی)

دو مشکل جدا بود:

1. **گوشه‌ی دهن بادکرده/گرد:** Catmull-Rom ذاتاً همه‌جا رو صاف می‌کنه، حتی
   گوشه‌ی تیز طبیعی دهن رو؛ `cv2.dilate` بعدی این گردی رو بیشتر هم می‌کنه.
   الان نقطه‌ی گوشه‌ی دهن (لندمارک ۶۱/۲۹۱ و معادل داخلی‌شون) قبل از رسم منحنی
   دوپلیکیت می‌شه (`_sharpen_corners`) تا اون‌جا کمتر باد کنه، و `MASK_GROW`
   تقریباً صفر شد (چون `refine_edges` که قبلاً فعال کردیم خودش مرز واقعی رو از
   روی فریم پیدا می‌کنه، دیگه نیازی به دیلیت اضافه‌ی از قبل نبود).
2. **رنگ کاملاً تخت/یکدست:** `opacity` کمی کم شد، `chroma_keep` برای
   liquid/stick بیشتر شد (بافت طبیعی لب بیشتر از زیر رنگ دیده می‌شه)، و
   `spec` این دو فینیش از منفی (تخت‌کننده) به یه مقدار خیلی کوچیکِ مثبت
   تغییر کرد (یه برق طبیعی خفیف، نه کاملاً مات).

اگه بعد از این هم حس «مصنوعی» موند، احتمالاً از رزولوشن پایین فریم ارسالی
میاد (`CAPTURE_W = 480` توی `app/static/review.html` و `live.html`، با
JPEG quality=۰.۸)؛ فشرده‌سازی شدید در این رزولوشن خودش بافت ریز لب رو از
بین می‌بره، مستقل از هر پارامتری که اینجا تنظیم بشه. بالا بردن `CAPTURE_W`
(مثلاً به ۶۴۰) و کیفیت JPEG (مثلاً ۰.۹) توی همون دو فایل، یه تست ساده‌ی
بعدیه.

## به‌روزرسانی سوم (بازخورد: باید دقیق مثل Snapchat روی خودِ لب بشینه، نه ماسک مصنوعی)

ریشه‌ی اصلیِ حس «ماسک» خودِ Catmull-Rom بود، نه فقط پارامترهاش:

- **کل مرحله‌ی spline حذف شد.** هر منحنی پارامتریک شکل واقعی لب (پیک‌های کمان
  کوپید، فرورفتگی‌ها، عدم‌تقارن جزئی) رو به یه فرم صاف/ژنریک نزدیک می‌کنه —
  همون چیزی که حس «ماسک روی لب» می‌ده. الان `_contour_px` مستقیم خودِ موقعیت
  لندمارک‌ها رو برمی‌گردونه (بدون بازسازی هندسی)؛ نرمی بصری کاملاً از
  بلور لبه‌ی خیلی کوچیک + guided filter میاد، نه از تغییر شکل نقاط. این
  دقیقاً همون رویکرد اپ‌های حرفه‌ای مثل Snapchatـه.
- **guided filter قوی‌تر شد** (شعاع ۰.۰۲→۰.۰۳۵ نسبت به عرض لب، eps
  ۰.۰۰۵→۰.۰۰۲) تا هر فریم قاطع‌تر به مرز واقعی رنگ لب/پوست توی خودِ تصویر
  بچسبه؛ همچنان فقط می‌تونه ماسک رو تنگ‌تر کنه نه بیرون‌تر.
- تابع‌های `_catmull_rom_closed` و `_sharpen_corners` (مخصوص بازخورد قبلی)
  کلاً حذف شدن؛ دیگه لازم نبودن.

اگه هنوز حس «چسبیده نبودن» موند، محتمل‌ترین دلیل باقی‌مونده دقت خودِ
لندمارک‌های MediaPipe در فریم‌های نویزی/کم‌نوره (که این کد کنترلی روش نداره)
یا همون محدودیت رزولوشن capture که در به‌روزرسانی قبلی اشاره شد.

## کالیبراسیون بیشتر (اختیاری)

اگه بعد از تست با دوربین هنوز حس کردی:
- **هنوز کمی بیرون می‌زنه** → `MASK_GROW` رو به `0.004` کمتر کن یا آستانه‌ی
  `sel = m > 0.25` رو بالاتر ببر (مثلاً `0.35`).
- **گوشه‌های لب رنگ نمی‌گیرن / لکه‌ی ناقص** → آستانه رو کمی پایین‌تر بیار
  (مثلاً `0.18`) یا `FEATHER_FRAC` رو کمی بیشتر کن.
- **هنوز پلاستیکی به‌نظر می‌رسه** → `chroma_keep` رو بیشتر ببر یا `opacity`
  فینیش مربوطه رو کم کن.
